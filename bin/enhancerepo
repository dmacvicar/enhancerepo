#!/usr/bin/env ruby
# == Synopsis
#
# enhancerepo: adds extra data to a repomd repository
#
# Author: Duncan Mac-Vicar P. <dmacvicar@suse.de>
#
# == Usage
#
# enhancerepo [OPTION] ... DIR
#
# -h, --help:
#    show help
#
# --index
#   Reindex the metadata and regenerates repomd.xml, even
#   if nothing was changed using enhancerepo.
#   Use this if you did manual changes to the metadata
#
# --benchmark
#   Show benchmark statistics at the end
#
# --outputdir
#   Generate metadata to a different directory
#
# --primary
#   Add data from rpm files and generate
#   primary.xml (EXPERIMENTAL)
#
# --sign keyid
#   Generates signature for the repository
#   using key keyid
#
# --updates
#   Add updates from *.updates files
#   and generate updateinfo.xml
#
# --generate-update pkg1,pkg2,...
#   Generates an update from the given package list
#   comparing package's last version changes.
#
# --updates-base-dir dir
#   Looks for package also in <dir>
#   Useful if you keep old packages in a different repos
#   and updates in this one.
#
# --indent
#   Generate indented xml. Default no.
#
# === SUSE specific repository data (suseinfo.xml):
#
# --expire time
#   Set repository expiration hint
#   (Can be used to detect dead mirrors)
# --repo-product prodname:
#    Adds product compatibility information
# --repo-keyword keyword
#    Tags repository with keyword
#
# === SUSE specific package data (susedata.xml)
#
# --eulas
#    Reads packagename.eula files and add the
#    information to susedata.xml
#
# --keywords
#    Reads packagename.keywords files and add
#    keyword metadata to susedata.xml
#
# --disk-usage
#    Reads rpm packages, generates disk usage information
#    on susedata.xml
#
# Note: your .eula or .keywords file will be added to
# a package if it matches its name. If you want to add
# the attributes to a specific package, name the file
# name-version, name-version-release or
# name-version-release.arch
#
# === Package deltas support
#
# --create-deltas[=num] (default 1)
#   Create [num] deltas for different versions of a package.
#   If there is foo-1.rpm, foo-2.rpm, foo-3.rpm, foo-4.rpm
#   num=1 will create a delta to go from version 3 to 4, while
#   num=2 will create one from 2 to 4 too.
#   This does not index the deltas. Use --deltas for that.
#
# --deltas
#   Reads all *.delta.rpm files and add the information to
#   deltainfo.xml.
#   This indexes existing deltas, but won't create them.
#   See --create-deltas for deltas creation.
#
# --debug
#   Show debug information
#
# === Product information support
#
# --products
#   Reads release packages and generating product information
#   in products.xml based on the information contained in the
#   .prod files included in the packages.
#
# DIR: The repo base directory ( where repodata/ directory is located )
#

$: << '../lib' 
$: << 'lib'

require 'enhance_repo/cli.rb'
